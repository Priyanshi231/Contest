#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	
	int t;
	cin>>t;
	
	while(t!=0){
	    int n;
        cin>>n;
        
        vector<int> a(n);
        
        for(int i=0; i<n; i++){
            cin>>a[i];
        }
        int mn = *min_element(a.begin(), a.end());
        int mx = *max_element(a.begin(), a.end());
        
        int c = 0;
        
        for(int i=0; i<n; i++){
            if(a[i] != mn && a[i] != mx){
                c++;
            }
        }
        
        cout<<c<<endl;
        t--;
	}
    
    
    
}
